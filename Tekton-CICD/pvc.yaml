apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ci-workspace-pvc
  namespace: tekton-ci
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard  # Change this to match your cluster's storage class
---
